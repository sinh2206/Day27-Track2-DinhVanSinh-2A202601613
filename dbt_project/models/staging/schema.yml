version: 2

models:
  - name: stg_orders
    description: Cleaned order records used by downstream marts.
    columns:
      - name: order_id
        data_tests:
          - not_null
          - unique
      - name: customer_id
        data_tests:
          - not_null
      - name: amount_usd
        data_tests:
          - not_null
      - name: currency
        data_tests:
          - accepted_values:
              arguments:
                values: ['USD', 'VND']
      - name: status
        data_tests:
          - accepted_values:
              arguments:
                values: ['pending', 'completed', 'refunded', 'cancelled']
      - name: order_date
        data_tests:
          - not_null

  - name: stg_customers
    description: Cleaned customer records with SCD tracking.
    columns:
      - name: customer_id
        data_tests:
          - not_null
      - name: is_active
        data_tests:
          - not_null

